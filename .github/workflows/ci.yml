on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  packages: write

env:
  PYTHON_VERSION: "3.13"
  UV_VERSION: "0.11.7"

jobs:
  pre-commit:
    runs-on: ubuntu-latest
    env:
      PRE_COMMIT_HOME: /home/runner/.pre-commit-cache
      PRE_COMMIT_VERSION: "4.5.1"
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: "true"
      - name: Configure pre-commit cache
        uses: actions/cache@v5
        with:
          path: ${{ env.PRE_COMMIT_HOME }}
          key: pre-commit-${{ runner.os }}-py${{ env.PYTHON_VERSION }}-${{ hashFiles('**/.pre-commit-config.yaml') }}
      - name: Run pre-commit hooks
        run: uvx "pre-commit==$PRE_COMMIT_VERSION" run --all-files --show-diff-on-failure --color=always

  test:
    runs-on: ubuntu-latest
    needs: [pre-commit]
    env:
      UV_LOCKED: "true"
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.1.0
        with:
          version: ${{ env.UV_VERSION }}
          enable-cache: "true"
      - name: Install Python dependencies
        run: uv sync
      - name: Python type checking
        run: uv run mypy .
      - name: Python tests
        run: COLUMNS=120 uv run pytest -vvv --color=yes

  bundle:
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 25
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm -C frontend ci
      - name: Build frontend
        env:
          FRONTEND_PATH: ticket_queue/frontend
        run: |
          npm -C frontend run build -- --emptyOutDir --outDir "$PWD/$FRONTEND_PATH"
          tree -a "$FRONTEND_PATH"
      - name: Disable custom build hook
        run: |
          rm frontend_build_hook.py
          rm -rf frontend/
          perl -0777 -i -pe 's/(?ms)\[tool\.hatch\.build\.hooks\.custom\].*?^\n//' pyproject.toml
          ! git diff --exit-code -- pyproject.toml
      - name: Generate frontend package file
        run: |
          >ticket_queue/_packaged_frontend.py cat << EOF
          import os.path

          PACKAGE_DIR = os.path.join(
              os.path.dirname(__file__),
              "frontend"
          )
          EOF
      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      - name: Configure Git worktree
        run: |
          worktree="$(mktemp -d)/bundled"

          if git ls-remote --exit-code --heads origin bundled >/dev/null; then
            git worktree add --track -b bundled "$worktree" origin/bundled
          else
            git worktree add -b bundled "$worktree"
          fi

          echo "BUNDLED_WORKTREE=$worktree" >> "$GITHUB_ENV"
      - name: Copy files to bundled worktree
        run: |
          rsync \
            --info=progress2 \
            -avv \
            --ignore-times --delete --delete-excluded \
            --filter 'P /.git' --exclude=/.git --filter=':- .gitignore' \
            . "$BUNDLED_WORKTREE/"
      - name: Commit and push bundled worktree
        working-directory: ${{ env.BUNDLED_WORKTREE }}
        run: |
          git add --all
          if git commit --quiet -m "Bundle for ${{ github.sha }}"; then
            git show --stat --color=always
            git push -u origin bundled
          else
            echo "No changes to commit."
          fi

  publish-container:
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - name: Checkout repo
        uses: actions/checkout@v6
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ghcr.io/${{ github.repository }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
      - name: Build and push container
        uses: docker/build-push-action@v7
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
